# 📚 FAQ Loader - Загрузчик базы знаний в Qdrant

Production-ready инструмент для загрузки FAQ из Excel в векторную базу данных Qdrant с использованием OpenAI embeddings.

## 🎯 Возможности

✅ Поддержка двух форматов Excel (простой и расширенный)  
✅ Автоматическое создание векторных представлений через OpenAI API  
✅ Параллельная обработка с контролем rate limits  
✅ Валидация данных с гибкими лимитами  
✅ Стабильные ID на основе содержимого  
✅ CLI для автоматизации  
✅ Детальное логирование  
✅ Поддержка удаленного Qdrant сервера  

## 📋 Поддерживаемые форматы Excel

### Формат 1: Простой (базовый)

```
ID | Категория | Вопрос (FAQ) | Ответ | Ключевые слова | Источник / ссылка | Дата актуализации
```

**Обязательные столбцы:**
- Вопрос (FAQ)
- Ответ

### Формат 2: Расширенный (юридический/медицинский)

```
ID | Категория | Вопрос (FAQ) | Краткий ответ | Развернутый ответ | 
Пошаговая инструкция | Список документов | Закон и статьи | 
Ключевые слова | Теги | Источник / ссылка | Дата актуализации
```

**Обязательные столбцы:**
- Вопрос (FAQ)
- Хотя бы один из: Ответ / Краткий ответ / Развернутый ответ

**Логика обработки ответов:**
1. Приоритет: Развернутый ответ → Ответ → Краткий ответ
2. Все доступные поля включаются в embedding для лучшего поиска
3. Все поля сохраняются в metadata для последующего использования

## 🚀 Быстрый старт

### 1. Установка

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка

Создайте `.env` файл:

```env
# OpenAI
OPENAI_API_KEY_ORIG=sk-your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# Qdrant
QDRANT_HOST=your-server.com
QDRANT_PORT=6333
QDRANT_COLLECTION=faq
```

**Примеры адресов Qdrant:**
- Локальный: `QDRANT_HOST=localhost`
- Сервер в сети: `QDRANT_HOST=192.168.1.100`
- Домен: `QDRANT_HOST=qdrant.mycompany.com`
- Qdrant Cloud: `QDRANT_HOST=xyz-example.aws.cloud.qdrant.io`

### 3. Запуск

```bash
# Интерактивный режим (с GUI)
python faq_loader.py

# Автоматический режим
python faq_loader.py --file data/faq.xlsx --auto-confirm

# С увеличенными лимитами для детальных FAQ
python faq_loader.py --file data/faq.xlsx --max-answer-length 15000 --auto-confirm
```

## 📝 Примеры использования

### Базовая загрузка

```bash
python faq_loader.py --file dom_ur_bot.xlsx --auto-confirm
```

### Юридический/медицинский контент (длинные ответы)

```bash
python faq_loader.py \
  --file legal_faq.xlsx \
  --max-answer-length 15000 \
  --max-question-length 2000 \
  --auto-confirm
```

### Полная замена данных (refresh mode)

```bash
python faq_loader.py \
  --file new_faq.xlsx \
  --mode refresh \
  --auto-confirm
```

### С кастомной конфигурацией

```bash
python faq_loader.py \
  --config config.yaml \
  --file faq.xlsx \
  --auto-confirm
```

### Автоматизация (без GUI)

```bash
python faq_loader.py \
  --file faq.xlsx \
  --no-gui \
  --auto-confirm \
  --workers 10 \
  --batch-size 50
```

### Для медленных систем

```bash
python faq_loader.py \
  --file faq.xlsx \
  --workers 2 \
  --batch-size 10
```

## ⚙️ Параметры командной строки

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `--file` | Путь к Excel файлу | Диалог выбора файла |
| `--config` | Путь к config.yaml | Из .env |
| `--mode` | Режим обновления: `upsert`, `refresh`, `incremental` | `upsert` |
| `--auto-confirm` | Без подтверждений | `false` |
| `--no-gui` | Отключить GUI (для автоматизации) | `false` |
| `--batch-size` | Размер батча для обработки | `20` |
| `--workers` | Количество параллельных workers | `5` |
| `--max-answer-length` | Максимальная длина ответа (символов) | `5000` |
| `--max-question-length` | Максимальная длина вопроса (символов) | `1000` |
| `--log-file` | Имя файла для логов | Автоматическое |

## 📊 Режимы обновления

### `upsert` (по умолчанию)
- Обновляет существующие записи по ID
- Добавляет новые записи
- **НЕ удаляет** отсутствующие в файле записи
- **Использовать:** для добавления новых FAQ без потери старых

### `refresh`
- Полностью очищает коллекцию
- Загружает все данные заново
- **Использовать:** когда нужна полная замена базы

### `incremental` (в разработке)
- Добавляет только новые записи
- Не обновляет существующие
- **Использовать:** для пополнения базы новыми FAQ

## 🔍 Логирование

Все логи сохраняются в папке `logs/` с именем вида:
```
logs/faq_loader_20251202_162829.log
```

Логи содержат:
- Детальную информацию о процессе
- Ошибки валидации
- Статистику API
- Информацию о загруженных записях

## 🛠️ Настройка лимитов

### Рекомендуемые значения по типу контента:

| Тип контента | max_answer_length | Пример |
|--------------|-------------------|--------|
| Простые FAQ (техподдержка) | 5,000 | "Как сбросить пароль?" |
| Средние FAQ (инструкции) | 10,000 | "Как настроить интеграцию?" |
| Детальные FAQ (юридические) | 15,000 | "Порядок наследования имущества" |
| Очень длинные | >15,000 | ⚠️ Разбить на несколько FAQ |

### Почему существуют лимиты?

1. **Технические ограничения:** OpenAI API имеет лимит ~8000 токенов (~30,000 символов)
2. **Качество поиска:** Слишком длинные тексты дают "размытые" векторы
3. **Стоимость:** Больше символов = больше токенов = выше стоимость
4. **Производительность:** Длинные тексты медленнее обрабатываются

## 📈 Мониторинг

После загрузки проверьте статистику:

```bash
# Через Qdrant API
curl http://your-server:6333/collections/faq

# Через Python
python -c "from qdrant_client import QdrantClient; c = QdrantClient('your-server', 6333); print(c.get_collection('faq'))"
```

## 🐛 Решение проблем

### Ошибка: "Connection refused"
```bash
# Проверьте что Qdrant запущен и доступен
curl http://your-server:6333/

# Проверьте адрес в .env
cat .env | grep QDRANT_HOST
```

### Ошибка: "Invalid API key"
```bash
# Проверьте ключ API в .env
cat .env | grep OPENAI_API_KEY

# Убедитесь что ключ правильный
echo $OPENAI_API_KEY_ORIG
```

### Ошибка: "Ответ слишком длинный"
```bash
# Увеличьте лимит
python faq_loader.py --file data.xlsx --max-answer-length 15000
```

### Ошибка: "UnicodeDecodeError" в .env
```
# Пересоздайте .env с правильной кодировкой UTF-8
# Используйте Notepad с "Сохранить как" → UTF-8 (без BOM)
```

## 📦 Структура проекта

```
.
├── faq_loader.py          # Основной скрипт
├── requirements.txt       # Python зависимости
├── config.yaml           # Конфигурация (опционально)
├── .env                  # Переменные окружения
├── logs/                 # Логи выполнения
│   └── faq_loader_*.log
└── README.md            # Эта документация
```

## 🔐 Безопасность

- **НЕ коммитьте** `.env` файл в git
- Добавьте в `.gitignore`:
  ```
  .env
  logs/
  *.log
  venv/
  ```
- Используйте отдельные API ключи для production и development
- Ограничьте доступ к Qdrant серверу через firewall

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в папке `logs/`
2. Убедитесь что все зависимости установлены
3. Проверьте доступность Qdrant и OpenAI API
4. Проверьте формат Excel файла

## 📄 Лицензия

MIT License - используйте свободно для коммерческих и некоммерческих проектов.
